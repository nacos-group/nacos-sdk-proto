// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v5.28.3
// source: ai/agentversionsummary.proto

/* eslint-disable */

/** Auto-generated by proto-generator. DO NOT EDIT. */

/**
 * metadata.type = "AgentVersionSummary"
 * Flattened from: AgentVersionSummary
 */
export interface AgentVersionSummary {
  version: string;
  labels: string[];
  protocols: string[];
  status: string;
  publishPipelineInfo: string;
  author: string;
  changeDescription: string;
  contentDigest: string;
  createTime: number;
  updateTime: number;
}

function createBaseAgentVersionSummary(): AgentVersionSummary {
  return {
    version: "",
    labels: [],
    protocols: [],
    status: "",
    publishPipelineInfo: "",
    author: "",
    changeDescription: "",
    contentDigest: "",
    createTime: 0,
    updateTime: 0,
  };
}

export const AgentVersionSummary: MessageFns<AgentVersionSummary> = {
  fromJSON(object: any): AgentVersionSummary {
    return {
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      labels: globalThis.Array.isArray(object?.labels) ? object.labels.map((e: any) => globalThis.String(e)) : [],
      protocols: globalThis.Array.isArray(object?.protocols)
        ? object.protocols.map((e: any) => globalThis.String(e))
        : [],
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      publishPipelineInfo: isSet(object.publishPipelineInfo) ? globalThis.String(object.publishPipelineInfo) : "",
      author: isSet(object.author) ? globalThis.String(object.author) : "",
      changeDescription: isSet(object.changeDescription) ? globalThis.String(object.changeDescription) : "",
      contentDigest: isSet(object.contentDigest) ? globalThis.String(object.contentDigest) : "",
      createTime: isSet(object.createTime) ? globalThis.Number(object.createTime) : 0,
      updateTime: isSet(object.updateTime) ? globalThis.Number(object.updateTime) : 0,
    };
  },

  toJSON(message: AgentVersionSummary): unknown {
    const obj: any = {};
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.labels?.length) {
      obj.labels = message.labels;
    }
    if (message.protocols?.length) {
      obj.protocols = message.protocols;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.publishPipelineInfo !== "") {
      obj.publishPipelineInfo = message.publishPipelineInfo;
    }
    if (message.author !== "") {
      obj.author = message.author;
    }
    if (message.changeDescription !== "") {
      obj.changeDescription = message.changeDescription;
    }
    if (message.contentDigest !== "") {
      obj.contentDigest = message.contentDigest;
    }
    if (message.createTime !== 0) {
      obj.createTime = Math.round(message.createTime);
    }
    if (message.updateTime !== 0) {
      obj.updateTime = Math.round(message.updateTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AgentVersionSummary>, I>>(base?: I): AgentVersionSummary {
    return AgentVersionSummary.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AgentVersionSummary>, I>>(object: I): AgentVersionSummary {
    const message = createBaseAgentVersionSummary();
    message.version = object.version ?? "";
    message.labels = object.labels?.map((e) => e) || [];
    message.protocols = object.protocols?.map((e) => e) || [];
    message.status = object.status ?? "";
    message.publishPipelineInfo = object.publishPipelineInfo ?? "";
    message.author = object.author ?? "";
    message.changeDescription = object.changeDescription ?? "";
    message.contentDigest = object.contentDigest ?? "";
    message.createTime = object.createTime ?? 0;
    message.updateTime = object.updateTime ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
