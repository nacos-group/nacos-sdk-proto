// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v5.28.3
// source: ai/agentcatalogentrypage.proto

/* eslint-disable */
import { AgentCatalogEntry } from "./agentcatalogentry";

/** Auto-generated by proto-generator. DO NOT EDIT. */

/**
 * metadata.type = "AgentCatalogEntryPage"
 * Flattened from: Page<AgentCatalogEntry>
 */
export interface AgentCatalogEntryPage {
  totalCount: number;
  pageNumber: number;
  pagesAvailable: number;
  pageItems: AgentCatalogEntry[];
}

function createBaseAgentCatalogEntryPage(): AgentCatalogEntryPage {
  return { totalCount: 0, pageNumber: 0, pagesAvailable: 0, pageItems: [] };
}

export const AgentCatalogEntryPage: MessageFns<AgentCatalogEntryPage> = {
  fromJSON(object: any): AgentCatalogEntryPage {
    return {
      totalCount: isSet(object.totalCount) ? globalThis.Number(object.totalCount) : 0,
      pageNumber: isSet(object.pageNumber) ? globalThis.Number(object.pageNumber) : 0,
      pagesAvailable: isSet(object.pagesAvailable) ? globalThis.Number(object.pagesAvailable) : 0,
      pageItems: globalThis.Array.isArray(object?.pageItems)
        ? object.pageItems.map((e: any) => AgentCatalogEntry.fromJSON(e))
        : [],
    };
  },

  toJSON(message: AgentCatalogEntryPage): unknown {
    const obj: any = {};
    if (message.totalCount !== 0) {
      obj.totalCount = Math.round(message.totalCount);
    }
    if (message.pageNumber !== 0) {
      obj.pageNumber = Math.round(message.pageNumber);
    }
    if (message.pagesAvailable !== 0) {
      obj.pagesAvailable = Math.round(message.pagesAvailable);
    }
    if (message.pageItems?.length) {
      obj.pageItems = message.pageItems.map((e) => AgentCatalogEntry.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AgentCatalogEntryPage>, I>>(base?: I): AgentCatalogEntryPage {
    return AgentCatalogEntryPage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AgentCatalogEntryPage>, I>>(object: I): AgentCatalogEntryPage {
    const message = createBaseAgentCatalogEntryPage();
    message.totalCount = object.totalCount ?? 0;
    message.pageNumber = object.pageNumber ?? 0;
    message.pagesAvailable = object.pagesAvailable ?? 0;
    message.pageItems = object.pageItems?.map((e) => AgentCatalogEntry.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
