// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v5.28.3
// source: ai/endpoint.proto

/* eslint-disable */

/** Auto-generated by proto-generator. DO NOT EDIT. */

/**
 * metadata.type = "Endpoint"
 * Flattened from: Endpoint
 */
export interface Endpoint {
  uri: string;
  transport: string;
  priority: number;
  weight: number;
  metadata: { [key: string]: string };
  healthy: boolean;
}

export interface Endpoint_MetadataEntry {
  key: string;
  value: string;
}

function createBaseEndpoint(): Endpoint {
  return { uri: "", transport: "", priority: 0, weight: 0, metadata: {}, healthy: false };
}

export const Endpoint: MessageFns<Endpoint> = {
  fromJSON(object: any): Endpoint {
    return {
      uri: isSet(object.uri) ? globalThis.String(object.uri) : "",
      transport: isSet(object.transport) ? globalThis.String(object.transport) : "",
      priority: isSet(object.priority) ? globalThis.Number(object.priority) : 0,
      weight: isSet(object.weight) ? globalThis.Number(object.weight) : 0,
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      healthy: isSet(object.healthy) ? globalThis.Boolean(object.healthy) : false,
    };
  },

  toJSON(message: Endpoint): unknown {
    const obj: any = {};
    if (message.uri !== "") {
      obj.uri = message.uri;
    }
    if (message.transport !== "") {
      obj.transport = message.transport;
    }
    if (message.priority !== 0) {
      obj.priority = Math.round(message.priority);
    }
    if (message.weight !== 0) {
      obj.weight = message.weight;
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    if (message.healthy !== false) {
      obj.healthy = message.healthy;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Endpoint>, I>>(base?: I): Endpoint {
    return Endpoint.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Endpoint>, I>>(object: I): Endpoint {
    const message = createBaseEndpoint();
    message.uri = object.uri ?? "";
    message.transport = object.transport ?? "";
    message.priority = object.priority ?? 0;
    message.weight = object.weight ?? 0;
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.healthy = object.healthy ?? false;
    return message;
  },
};

function createBaseEndpoint_MetadataEntry(): Endpoint_MetadataEntry {
  return { key: "", value: "" };
}

export const Endpoint_MetadataEntry: MessageFns<Endpoint_MetadataEntry> = {
  fromJSON(object: any): Endpoint_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: Endpoint_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Endpoint_MetadataEntry>, I>>(base?: I): Endpoint_MetadataEntry {
    return Endpoint_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Endpoint_MetadataEntry>, I>>(object: I): Endpoint_MetadataEntry {
    const message = createBaseEndpoint_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
