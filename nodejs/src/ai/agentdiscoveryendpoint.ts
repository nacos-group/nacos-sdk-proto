// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v5.28.3
// source: ai/agentdiscoveryendpoint.proto

/* eslint-disable */
import { RuntimeVersionBinding } from "./runtimeversionbinding";

/** Auto-generated by proto-generator. DO NOT EDIT. */

/**
 * metadata.type = "AgentDiscoveryEndpoint"
 * Flattened from: AgentDiscoveryEndpoint -> Endpoint
 */
export interface AgentDiscoveryEndpoint {
  uri: string;
  transport: string;
  priority: number;
  weight: number;
  metadata: { [key: string]: string };
  healthy: boolean;
  bindings: RuntimeVersionBinding[];
}

export interface AgentDiscoveryEndpoint_MetadataEntry {
  key: string;
  value: string;
}

function createBaseAgentDiscoveryEndpoint(): AgentDiscoveryEndpoint {
  return { uri: "", transport: "", priority: 0, weight: 0, metadata: {}, healthy: false, bindings: [] };
}

export const AgentDiscoveryEndpoint: MessageFns<AgentDiscoveryEndpoint> = {
  fromJSON(object: any): AgentDiscoveryEndpoint {
    return {
      uri: isSet(object.uri) ? globalThis.String(object.uri) : "",
      transport: isSet(object.transport) ? globalThis.String(object.transport) : "",
      priority: isSet(object.priority) ? globalThis.Number(object.priority) : 0,
      weight: isSet(object.weight) ? globalThis.Number(object.weight) : 0,
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      healthy: isSet(object.healthy) ? globalThis.Boolean(object.healthy) : false,
      bindings: globalThis.Array.isArray(object?.bindings)
        ? object.bindings.map((e: any) => RuntimeVersionBinding.fromJSON(e))
        : [],
    };
  },

  toJSON(message: AgentDiscoveryEndpoint): unknown {
    const obj: any = {};
    if (message.uri !== "") {
      obj.uri = message.uri;
    }
    if (message.transport !== "") {
      obj.transport = message.transport;
    }
    if (message.priority !== 0) {
      obj.priority = Math.round(message.priority);
    }
    if (message.weight !== 0) {
      obj.weight = message.weight;
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    if (message.healthy !== false) {
      obj.healthy = message.healthy;
    }
    if (message.bindings?.length) {
      obj.bindings = message.bindings.map((e) => RuntimeVersionBinding.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AgentDiscoveryEndpoint>, I>>(base?: I): AgentDiscoveryEndpoint {
    return AgentDiscoveryEndpoint.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AgentDiscoveryEndpoint>, I>>(object: I): AgentDiscoveryEndpoint {
    const message = createBaseAgentDiscoveryEndpoint();
    message.uri = object.uri ?? "";
    message.transport = object.transport ?? "";
    message.priority = object.priority ?? 0;
    message.weight = object.weight ?? 0;
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.healthy = object.healthy ?? false;
    message.bindings = object.bindings?.map((e) => RuntimeVersionBinding.fromPartial(e)) || [];
    return message;
  },
};

function createBaseAgentDiscoveryEndpoint_MetadataEntry(): AgentDiscoveryEndpoint_MetadataEntry {
  return { key: "", value: "" };
}

export const AgentDiscoveryEndpoint_MetadataEntry: MessageFns<AgentDiscoveryEndpoint_MetadataEntry> = {
  fromJSON(object: any): AgentDiscoveryEndpoint_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: AgentDiscoveryEndpoint_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AgentDiscoveryEndpoint_MetadataEntry>, I>>(
    base?: I,
  ): AgentDiscoveryEndpoint_MetadataEntry {
    return AgentDiscoveryEndpoint_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AgentDiscoveryEndpoint_MetadataEntry>, I>>(
    object: I,
  ): AgentDiscoveryEndpoint_MetadataEntry {
    const message = createBaseAgentDiscoveryEndpoint_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
