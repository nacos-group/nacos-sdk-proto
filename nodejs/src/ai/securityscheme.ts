// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v7.34.0
// source: ai/securityscheme.proto

/* eslint-disable */

/** Auto-generated by proto-generator. DO NOT EDIT. */

/**
 * metadata.type = "SecurityScheme"
 * Flattened from: SecurityScheme
 */
export interface SecurityScheme {
  threshold: number;
  loadFactor: number;
}

/**
 * metadata.type = "McpSecurityScheme"
 * Flattened from: SecurityScheme
 */
export interface McpSecurityScheme {
  id: string;
  type: string;
  scheme: string;
  in: string;
  name: string;
  defaultCredential: string;
}

function createBaseSecurityScheme(): SecurityScheme {
  return { threshold: 0, loadFactor: 0 };
}

export const SecurityScheme: MessageFns<SecurityScheme> = {
  fromJSON(object: any): SecurityScheme {
    return {
      threshold: isSet(object.threshold) ? globalThis.Number(object.threshold) : 0,
      loadFactor: isSet(object.loadFactor) ? globalThis.Number(object.loadFactor) : 0,
    };
  },

  toJSON(message: SecurityScheme): unknown {
    const obj: any = {};
    if (message.threshold !== 0) {
      obj.threshold = Math.round(message.threshold);
    }
    if (message.loadFactor !== 0) {
      obj.loadFactor = message.loadFactor;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SecurityScheme>, I>>(base?: I): SecurityScheme {
    return SecurityScheme.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SecurityScheme>, I>>(object: I): SecurityScheme {
    const message = createBaseSecurityScheme();
    message.threshold = object.threshold ?? 0;
    message.loadFactor = object.loadFactor ?? 0;
    return message;
  },
};

function createBaseMcpSecurityScheme(): McpSecurityScheme {
  return { id: "", type: "", scheme: "", in: "", name: "", defaultCredential: "" };
}

export const McpSecurityScheme: MessageFns<McpSecurityScheme> = {
  fromJSON(object: any): McpSecurityScheme {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      scheme: isSet(object.scheme) ? globalThis.String(object.scheme) : "",
      in: isSet(object.in) ? globalThis.String(object.in) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      defaultCredential: isSet(object.defaultCredential) ? globalThis.String(object.defaultCredential) : "",
    };
  },

  toJSON(message: McpSecurityScheme): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.scheme !== "") {
      obj.scheme = message.scheme;
    }
    if (message.in !== "") {
      obj.in = message.in;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.defaultCredential !== "") {
      obj.defaultCredential = message.defaultCredential;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<McpSecurityScheme>, I>>(base?: I): McpSecurityScheme {
    return McpSecurityScheme.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<McpSecurityScheme>, I>>(object: I): McpSecurityScheme {
    const message = createBaseMcpSecurityScheme();
    message.id = object.id ?? "";
    message.type = object.type ?? "";
    message.scheme = object.scheme ?? "";
    message.in = object.in ?? "";
    message.name = object.name ?? "";
    message.defaultCredential = object.defaultCredential ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
