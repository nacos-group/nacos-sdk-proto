// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v5.28.3
// source: ai/mcpresourcespecification.proto

/* eslint-disable */
import { EncryptObject } from "./encryptobject";

/** Auto-generated by proto-generator. DO NOT EDIT. */

/**
 * metadata.type = "McpResourceSpecification"
 * Flattened from: McpResourceSpecification
 */
export interface McpResourceSpecification {
  specificationType: string;
  encryptData: EncryptObject | undefined;
  resources: any[];
  resourceTemplates: any[];
  extensions: { [key: string]: any | undefined };
}

export interface McpResourceSpecification_ExtensionsEntry {
  key: string;
  value: any | undefined;
}

function createBaseMcpResourceSpecification(): McpResourceSpecification {
  return { specificationType: "", encryptData: undefined, resources: [], resourceTemplates: [], extensions: {} };
}

export const McpResourceSpecification: MessageFns<McpResourceSpecification> = {
  fromJSON(object: any): McpResourceSpecification {
    return {
      specificationType: isSet(object.specificationType) ? globalThis.String(object.specificationType) : "",
      encryptData: isSet(object.encryptData) ? EncryptObject.fromJSON(object.encryptData) : undefined,
      resources: globalThis.Array.isArray(object?.resources) ? [...object.resources] : [],
      resourceTemplates: globalThis.Array.isArray(object?.resourceTemplates) ? [...object.resourceTemplates] : [],
      extensions: isObject(object.extensions)
        ? (globalThis.Object.entries(object.extensions) as [string, any][]).reduce(
          (acc: { [key: string]: any | undefined }, [key, value]: [string, any]) => {
            acc[key] = value as any | undefined;
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: McpResourceSpecification): unknown {
    const obj: any = {};
    if (message.specificationType !== "") {
      obj.specificationType = message.specificationType;
    }
    if (message.encryptData !== undefined) {
      obj.encryptData = EncryptObject.toJSON(message.encryptData);
    }
    if (message.resources?.length) {
      obj.resources = message.resources;
    }
    if (message.resourceTemplates?.length) {
      obj.resourceTemplates = message.resourceTemplates;
    }
    if (message.extensions) {
      const entries = globalThis.Object.entries(message.extensions) as [string, any | undefined][];
      if (entries.length > 0) {
        obj.extensions = {};
        entries.forEach(([k, v]) => {
          obj.extensions[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<McpResourceSpecification>, I>>(base?: I): McpResourceSpecification {
    return McpResourceSpecification.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<McpResourceSpecification>, I>>(object: I): McpResourceSpecification {
    const message = createBaseMcpResourceSpecification();
    message.specificationType = object.specificationType ?? "";
    message.encryptData = (object.encryptData !== undefined && object.encryptData !== null)
      ? EncryptObject.fromPartial(object.encryptData)
      : undefined;
    message.resources = object.resources?.map((e) => e) || [];
    message.resourceTemplates = object.resourceTemplates?.map((e) => e) || [];
    message.extensions = (globalThis.Object.entries(object.extensions ?? {}) as [string, any | undefined][]).reduce(
      (acc: { [key: string]: any | undefined }, [key, value]: [string, any | undefined]) => {
        if (value !== undefined) {
          acc[key] = value;
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseMcpResourceSpecification_ExtensionsEntry(): McpResourceSpecification_ExtensionsEntry {
  return { key: "", value: undefined };
}

export const McpResourceSpecification_ExtensionsEntry: MessageFns<McpResourceSpecification_ExtensionsEntry> = {
  fromJSON(object: any): McpResourceSpecification_ExtensionsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object?.value) ? object.value : undefined,
    };
  },

  toJSON(message: McpResourceSpecification_ExtensionsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<McpResourceSpecification_ExtensionsEntry>, I>>(
    base?: I,
  ): McpResourceSpecification_ExtensionsEntry {
    return McpResourceSpecification_ExtensionsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<McpResourceSpecification_ExtensionsEntry>, I>>(
    object: I,
  ): McpResourceSpecification_ExtensionsEntry {
    const message = createBaseMcpResourceSpecification_ExtensionsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
