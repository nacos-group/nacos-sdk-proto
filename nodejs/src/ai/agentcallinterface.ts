// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v5.28.3
// source: ai/agentcallinterface.proto

/* eslint-disable */
import { Endpoint } from "./endpoint";

/** Auto-generated by proto-generator. DO NOT EDIT. */

/**
 * metadata.type = "AgentCallInterface"
 * Flattened from: AgentCallInterface
 */
export interface AgentCallInterface {
  protocol: string;
  protocolVersion: string;
  descriptorMediaType: string;
  nativeDescriptor: any | undefined;
  endpointSourceOrder: string[];
  declaredEndpoints: Endpoint[];
}

function createBaseAgentCallInterface(): AgentCallInterface {
  return {
    protocol: "",
    protocolVersion: "",
    descriptorMediaType: "",
    nativeDescriptor: undefined,
    endpointSourceOrder: [],
    declaredEndpoints: [],
  };
}

export const AgentCallInterface: MessageFns<AgentCallInterface> = {
  fromJSON(object: any): AgentCallInterface {
    return {
      protocol: isSet(object.protocol) ? globalThis.String(object.protocol) : "",
      protocolVersion: isSet(object.protocolVersion) ? globalThis.String(object.protocolVersion) : "",
      descriptorMediaType: isSet(object.descriptorMediaType) ? globalThis.String(object.descriptorMediaType) : "",
      nativeDescriptor: isSet(object?.nativeDescriptor) ? object.nativeDescriptor : undefined,
      endpointSourceOrder: globalThis.Array.isArray(object?.endpointSourceOrder)
        ? object.endpointSourceOrder.map((e: any) => globalThis.String(e))
        : [],
      declaredEndpoints: globalThis.Array.isArray(object?.declaredEndpoints)
        ? object.declaredEndpoints.map((e: any) => Endpoint.fromJSON(e))
        : [],
    };
  },

  toJSON(message: AgentCallInterface): unknown {
    const obj: any = {};
    if (message.protocol !== "") {
      obj.protocol = message.protocol;
    }
    if (message.protocolVersion !== "") {
      obj.protocolVersion = message.protocolVersion;
    }
    if (message.descriptorMediaType !== "") {
      obj.descriptorMediaType = message.descriptorMediaType;
    }
    if (message.nativeDescriptor !== undefined) {
      obj.nativeDescriptor = message.nativeDescriptor;
    }
    if (message.endpointSourceOrder?.length) {
      obj.endpointSourceOrder = message.endpointSourceOrder;
    }
    if (message.declaredEndpoints?.length) {
      obj.declaredEndpoints = message.declaredEndpoints.map((e) => Endpoint.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AgentCallInterface>, I>>(base?: I): AgentCallInterface {
    return AgentCallInterface.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AgentCallInterface>, I>>(object: I): AgentCallInterface {
    const message = createBaseAgentCallInterface();
    message.protocol = object.protocol ?? "";
    message.protocolVersion = object.protocolVersion ?? "";
    message.descriptorMediaType = object.descriptorMediaType ?? "";
    message.nativeDescriptor = object.nativeDescriptor ?? undefined;
    message.endpointSourceOrder = object.endpointSourceOrder?.map((e) => e) || [];
    message.declaredEndpoints = object.declaredEndpoints?.map((e) => Endpoint.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
