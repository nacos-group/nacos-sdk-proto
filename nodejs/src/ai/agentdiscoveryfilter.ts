// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v5.28.3
// source: ai/agentdiscoveryfilter.proto

/* eslint-disable */

/** Auto-generated by proto-generator. DO NOT EDIT. */

/**
 * metadata.type = "AgentDiscoveryFilter"
 * Flattened from: AgentDiscoveryFilter
 */
export interface AgentDiscoveryFilter {
  protocols: string[];
  protocolVersion: string;
  transports: string[];
  endpointSources: string[];
  metadataSelector: { [key: string]: string };
}

export interface AgentDiscoveryFilter_MetadataSelectorEntry {
  key: string;
  value: string;
}

function createBaseAgentDiscoveryFilter(): AgentDiscoveryFilter {
  return { protocols: [], protocolVersion: "", transports: [], endpointSources: [], metadataSelector: {} };
}

export const AgentDiscoveryFilter: MessageFns<AgentDiscoveryFilter> = {
  fromJSON(object: any): AgentDiscoveryFilter {
    return {
      protocols: globalThis.Array.isArray(object?.protocols)
        ? object.protocols.map((e: any) => globalThis.String(e))
        : [],
      protocolVersion: isSet(object.protocolVersion) ? globalThis.String(object.protocolVersion) : "",
      transports: globalThis.Array.isArray(object?.transports)
        ? object.transports.map((e: any) => globalThis.String(e))
        : [],
      endpointSources: globalThis.Array.isArray(object?.endpointSources)
        ? object.endpointSources.map((e: any) => globalThis.String(e))
        : [],
      metadataSelector: isObject(object.metadataSelector)
        ? (globalThis.Object.entries(object.metadataSelector) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: AgentDiscoveryFilter): unknown {
    const obj: any = {};
    if (message.protocols?.length) {
      obj.protocols = message.protocols;
    }
    if (message.protocolVersion !== "") {
      obj.protocolVersion = message.protocolVersion;
    }
    if (message.transports?.length) {
      obj.transports = message.transports;
    }
    if (message.endpointSources?.length) {
      obj.endpointSources = message.endpointSources;
    }
    if (message.metadataSelector) {
      const entries = globalThis.Object.entries(message.metadataSelector) as [string, string][];
      if (entries.length > 0) {
        obj.metadataSelector = {};
        entries.forEach(([k, v]) => {
          obj.metadataSelector[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AgentDiscoveryFilter>, I>>(base?: I): AgentDiscoveryFilter {
    return AgentDiscoveryFilter.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AgentDiscoveryFilter>, I>>(object: I): AgentDiscoveryFilter {
    const message = createBaseAgentDiscoveryFilter();
    message.protocols = object.protocols?.map((e) => e) || [];
    message.protocolVersion = object.protocolVersion ?? "";
    message.transports = object.transports?.map((e) => e) || [];
    message.endpointSources = object.endpointSources?.map((e) => e) || [];
    message.metadataSelector = (globalThis.Object.entries(object.metadataSelector ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseAgentDiscoveryFilter_MetadataSelectorEntry(): AgentDiscoveryFilter_MetadataSelectorEntry {
  return { key: "", value: "" };
}

export const AgentDiscoveryFilter_MetadataSelectorEntry: MessageFns<AgentDiscoveryFilter_MetadataSelectorEntry> = {
  fromJSON(object: any): AgentDiscoveryFilter_MetadataSelectorEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: AgentDiscoveryFilter_MetadataSelectorEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AgentDiscoveryFilter_MetadataSelectorEntry>, I>>(
    base?: I,
  ): AgentDiscoveryFilter_MetadataSelectorEntry {
    return AgentDiscoveryFilter_MetadataSelectorEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AgentDiscoveryFilter_MetadataSelectorEntry>, I>>(
    object: I,
  ): AgentDiscoveryFilter_MetadataSelectorEntry {
    const message = createBaseAgentDiscoveryFilter_MetadataSelectorEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
